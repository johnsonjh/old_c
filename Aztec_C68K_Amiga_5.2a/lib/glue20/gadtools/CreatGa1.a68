;:ts=8

; jd 8/28/91 new
; CreateGadget is glue only, not an entry in gadtools.library
	xref	_GadToolsBase
	xdef	_CreateGadget
_CreateGadget:
	movem.l	a6/a2,-(sp)
	move.l	_GadToolsBase,a6
	lea	12(sp),a2
	movem.l	(a2)+,d0/a0-a1
	jsr	-30(a6)			;CreateGadgetA
	movem.l	(sp)+,a6/a2
	rts
